# project-demo-template
Template for public project demo frontends (Pico.css + vanilla JS). Includes clean static file structure, API wrapper pattern, dataset selector scaffolding, and output rendering utilities. Designed for pairing with a privately hosted backend API for safe portfolio demos.
